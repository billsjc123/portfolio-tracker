# 2026-07-06

## 下午：诊断 GitHub Actions Run #8 失败 + 重跑排队久

### 现象
- 截图：run #8 (28769322945) "Scheduled" 4秒, main, commit 8901f26
- 用户重跑后一直 queue 等待
- repo: billsjc123/portfolio-tracker (public)
- owner: billsjc123 (个人 Free 账户)
- 上次成功: #7 (28657749710), 7月3日 19:31 北京时间, 17秒

### 诊断
- 同样的 commit 8901f26,中间没新 commit,workflow 也没改动 → 不是代码/workflow 问题
- GitHub Status 全绿 → 不是 GitHub 平台 incident
- cron `0 1,9 * * 1-5` (UTC 1:00, 9:00) = 北京时间 9:00 / 17:00 = 整点高峰期
- run #8 实际触发 7月6日 5:11 UTC(北京时间 13:11),不在 cron 计划里,可能是手动触发
- 4秒失败 + 重跑一直 queue → 高峰期 free 账户 fair-use throttling,导致 runner 分配超时

### 建议(已回复用户)
1. 改 cron 避开整点高峰(7/17 偏移): `7 1,9 * * 1-5` 或 `17 1,9 * * 1-5`
2. 不要在高峰期重跑,等非高峰(UTC 凌晨/晚上)再试
3. 急用就本地跑 `run-and-sync.sh`(mac 自动化任务,automation-1782386906308)

### 改进点
- 等用户确认后,可以主动改 `.github/workflows/update-data.yml` 的 cron
